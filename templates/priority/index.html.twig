{% extends 'base.html.twig' %}

{% block title %}Priority index{% endblock %}

{% block body %}
    <h1>Priority index</h1>
    <a href="{{ path('task_index') }}">lien vers task</a>
    <br>
    <br>
    <a href="{{ path('priority_index') }}">lien vers priority</a>
    <br>
    <br>
    <a href="{{ path('image_index') }}">lien vers image</a>
    <br>
    <br>
    <a href="{{ path('category_index') }}">lien vers priority</a>
    <br>
    <br>
    <a href="{{ path('tasklist_index') }}">lien vers tasklist</a>
    <br>
    <br>
    <a href="{{ path('listitem_index') }}">lien vers listitem</a>
    <br>
    <br>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>Value</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for priority in priorities %}
            <tr>
                <td>{{ priority.id }}</td>
                <td>{{ priority.name }}</td>
                <td>{{ priority.value }}</td>
                <td>
                    <a href="{{ path('priority_show', {'id': priority.id}) }}">show</a>
                    <a href="{{ path('priority_edit', {'id': priority.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('priority_new') }}">Create new</a>
{% endblock %}
